## 样式重置

1. 让非`ie`浏览器默认也显示垂直滚动条，防止因滚动条引起的闪烁

   ```html { overflow-y: scroll; }```
   
   ## 商品详情
   
   1. 需求：实现放大镜
   2. 逻辑
      1. 封装函数，再调用该函数
      2. 获取元素对象
      3. 鼠标移入事件
         1. 创建一个div作为蒙版，并添加蒙版的类名为其添加对应的样式
         2. 创建一个div作为显示放大区域
            1. 添加图片，设置图片大小
            2. 绝对定位
         3. 鼠标移动事件
            1. 蒙版定位
               1. 鼠标与浏览器距离
               2. 鼠标与蒙版距离
               3. 元素与浏览器距离
            2. 临界值判断
            3. 计算比例
            4. 确定大图区域图片定位
      4. 鼠标移出事件
         1. 鼠标移出，移除蒙版



1. 需求
   1. 实现点击切换
   2. 每次走两张的长度
2. 逻辑
   1. 设置`li`的内边距，`img`的边框
   2. 封装函数，调用函数
   3. 获取元素对象
   4. 右按钮点击事件(向左移)
      1. 变量
         1. 总共可以移动的长度
         2. 已经走过的长度
         3. 每次移动的距离
      2. 逻辑判断
         1. 如果总共可以走的长度-已经走过的长度(也就是剩下未走的长度)>2个`li`的长度，那么走两张图的宽度
         2. 否则走一张图的长度
      3. 设置列表移动

## 缩略图点击

1. 逻辑
   1. 获取元素对象
      1. 获取这组缩略图元素对象
      2. 小图区域
      3. 大图区域
   2. 遍历绑定点击事件
      1. 获取这个li下的img的src属性
      2. 把这个src赋给小图区域的img
      3. 把这个src赋给大图区域的img

## 动态加载数据

逻辑

1. 在全局引入数据
2. 封装函数，调用函数
3. 获取元素对象
   1. 循环
      1. 创建新的`li`节点
      2. 给`li`节点添加`src`属性
      3. 将`li`节点添加到列表中
   2. 获取添加进去的新元素组
      1. 给每个li绑定点击事件
      2. 小图区域获得的是小图
      3. 大图区域获得的是大图
   3. 获取小图区域，大图区域



## 详情筛选

1. 逻辑
   1. 在布局中添加一个div用于插入元素
   2. 创建一个数组，用于保存被选择的项
   3. 获取dl这组对象
   4. 对这组dl进行遍历
      1. 给每一个dl添加一个index属性
      2. 获取每一组的所有dd
      3. 默认情况下第一个dd的颜色是红色，所以把第一个设为红色
      4. 遍历每组中获得的dd
         1. 点击了某个元素，清空，再给目标元素颜色
         2. 根据其父元素index的值来确定把该项插在数组的第几项
      5. 获取元素对象
      6. 点完之后再把这一项添加到数组中
      7. 遍历数组
         1. 创建标签
         2. 把输入中的内容写入到标签中
         3. 在元素中添加标签

## Tab切换

逻辑

1. 布局
   1. 整个的div
   2. 一个div放标题
      1. 其中一个标题有默认样式
   3. 列表放内容，默认隐藏
      1. 其中一个li显示
2. 获取元素对象
   1. 标题
   2. li
3. 给标题绑定点击事件
   1. 清空所有标题的样式
   2. 给目标标题样式
   3. 让所有li隐藏，让目标li显示

需求：面向对象方式的tab切换

思路

1. 布局先写好
   1. 我之前的理解就是创建新的按钮，新的内容，然后插入到布局中
   2. 这里是先写好布局，写好多少个按钮，多少个内容
2. new出来的对象是一整个tab切换，在这个构造函数中只是增加了这个tab切换的切换效果

逻辑

1. 封装构造函数
   1. 传入tab切换中的按钮，内容区组
2. 给这个对象添加按钮属性，内容属性
3. 给按钮绑定点击事件
   1. 调用对象本身原型链上的函数
      1. 遍历按钮
         1. 给所有按钮类名为空
      2. 给目标按钮类名